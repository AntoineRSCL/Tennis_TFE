{% extends 'base.html.twig' %}

{% block title %}Nous contacter{% endblock %}

{% block body %}
    <div class="contact">
        <div class="imgContact">
            <h1>Nous Contacter</h1>
            <img src="/images/contactPage.jpg" alt="Image pour le contact">
            <div class="shapedividers_com-9608"></div>
        </div>
        <div class="formContact">
            <div class="shapedividers_com-9608"></div>
            <div class="contactContainer">
                <div class="leftContact">
                    <p class="important">Votre satisfaction est notre priorité, et nous sommes impatients de vous aider à profiter pleinement de votre expérience au tennis !</p>
                    <p>Vous avez des questions concernant notre site, des préoccupations sur votre réservation, ou peut-être un problème à signaler ? N'hésitez pas à nous contacter dès maintenant !</p>
                    <p>Que ce soit pour obtenir des informations supplémentaires sur nos installations, nos horaires, ou pour signaler un souci technique, notre équipe dédiée est là pour vous accompagner et vous répondre dans les plus brefs délais.</p>
                </div>
                <div class="rightContact">
                    <div class="formContacts">
                        {% for label, messages in app.flashes %}
                            <div class="alertContact alert-{{ label }}">
                                {% for message in messages %}
                                    <p>{{ message }}</p>
                                {% endfor %}
                            </div>
                        {% endfor %}
                        <form method="post">
                            {{ form_start(form) }}
                            <div class="inputContact">
                                {{ form_widget(form.lastname, {'attr': {'class': 'form-control', 'autocomplete': 'off'}}) }}
                                {{ form_errors(form.lastname) }}
                            </div>
                            <div class="inputContact">
                                {{ form_widget(form.firstName, {'attr': {'class': 'form-control', 'autocomplete': 'off'}}) }}
                                {{ form_errors(form.firstName) }}
                            </div>
                            <div class="inputContact">
                                {{ form_widget(form.email, {'attr': {'class': 'form-control', 'autocomplete': 'off'}}) }}
                                {{ form_errors(form.email) }}
                            </div>
                            <div class="inputContact">
                                {{ form_widget(form.subject, {'attr': {'class': 'form-control', 'autocomplete': 'off'}}) }}
                                {{ form_errors(form.subject) }}
                            </div>
                            <div class="inputContactMessage">
                                {{ form_widget(form.message, {'attr': {'class': 'form-control', 'autocomplete': 'off'}}) }}
                                {{ form_errors(form.message) }}
                            </div>
                            <div class="submitContact">
                                <div class="submitBtn">
                                    <button type="submit" class="submitBtn">Envoyer</button>
                                </div>
                            </div>
                            {{ form_end(form) }}
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <div class="mapContact">
            <div class="shapedividers_com-9608 return"></div>
            <div id="mapContact"></div>
            <div class="shapedividers_com-9608"></div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script>
        // Initialisation de la carte centrée sur le Parc de Wolvendael (coordonnées GPS : 50.8027, 4.3425)
        var map = L.map('mapContact', {
            zoomControl: false // Désactive les boutons + et -
        }).setView([50.8027, 4.3425], 15); // Niveau de zoom initial: 15

        // Charger et afficher les tuiles OpenStreetMap
        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
            attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
        }).addTo(map);

        // Ajouter un marqueur sur les coordonnées du Parc de Wolvendael
        var marker = L.marker([50.8027, 4.3425]).addTo(map)
            .bindPopup('Parc de Wolvendael, Uccle') // Popup lorsque l'on clique sur le marqueur
            .openPopup(); // Popup ouvert par défaut

    </script>

{% endblock %}
